<h1> All Notes </h1>
<%= button_to "New Note", new_note_path, method: "get" %>
<% @notes.each do |note| %>
  <ul>
    <li>
      <div style="color: #AFA5C6;"> <%=link_to note.title, note_path(note) %> <%= button_to "Edit", edit_note_path(note), method: :get %>
      <%= button_to "Delete", note_path(note), method: "delete" %></div>
    </li>
  </ul>
<% end %>
